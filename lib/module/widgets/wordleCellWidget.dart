import 'package:flutter/material.dart';

class WordleCellWidget extends StatelessWidget {
  const WordleCellWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
